import AdminWrapper from "@/app/(admin)/admin/_components/wrappers/AdminWrapper";
import React from "react";

type Props = {};

export default function NewListingSettingsPage({}: Props) {
  return (
    <AdminWrapper>
      <div className="w-full h-full flex items-start justify-center">d</div>
    </AdminWrapper>
  );
}
